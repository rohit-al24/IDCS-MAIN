// Unprotected wrapper for faculty verification.
// Re-exports the existing VerifyQuestions component so we can expose it on a route without any ProtectedRoute wrapper.
import VerifyQuestions from "./VerifyQuestions";
export default VerifyQuestions;
